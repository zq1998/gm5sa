<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zq.gm5sa">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/upc"
        android:label="UpcTown"
        android:roundIcon="@mipmap/upc"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".shengyuan" android:launchMode="singleTask"/>
        <activity android:name=".xingbie_Activity" android:launchMode="singleTask"/>
        <activity android:name=".xuesheng_Activity" android:launchMode="singleTask"/>
        <activity android:name=".xiaofei_Activity" android:launchMode="singleTask"/>
        <activity android:name=".xueba_Activity" android:launchMode="singleTask"/>
        <activity android:name=".zhaosheng_Activity" android:launchMode="singleTask"/>
        <activity android:name=".jiuye_Activity" android:launchMode="singleTask">
        </activity>
    </application>

</manifest>